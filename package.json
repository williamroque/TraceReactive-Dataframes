{
  "name": "@tracereactive/dataframes",
  "version": "1.0.0",
  "description": "TraceReactive Dataframes package",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js --platform=browser --format=iife --global-name=__TraceReactive_Package && esbuild src/ui.ts --bundle --outfile=dist/ui.js --platform=browser --format=iife --external:react --external:react-dom",
    "build:trpkg": "npm run build && node scripts/build-trpkg.js",
    "dev": "npm run build -- --watch"
  },
  "devDependencies": {
    "@tracereactive/types": "file:../types",
    "adm-zip": "^0.5.10",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "arquero": "^5.4.1",
    "xlsx": "^0.18.5"
  }
}
